---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Restart prometheus-nginxlog-exporter
  ansible.builtin.systemd:
    name: "{{ nginxlog_exporter_service }}"
    state: restarted
    daemon_reload: true

